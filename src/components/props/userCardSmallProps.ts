type userCardSmallProps = {
    userId: string
    first: string
    last: string
    image: string
    affiliation?: {
        orgName: string
        userTitle: string
    }
    avgRatings: string
}

export default userCardSmallProps
