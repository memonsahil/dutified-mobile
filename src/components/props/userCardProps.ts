type userCardProps = {
    first: string
    last: string
    image: string
    projectsCreated: string
    jobsCreated: string
    projectsWorked: string
    jobsWorked: string
}

export default userCardProps
