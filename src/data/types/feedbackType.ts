type feedbackType = {
    userId: string
    userName: string
    userImage: string
    feedbackId: string
    feedbackTitle: string
    feedback: string
    rating: string
    feedbackDate: Date
}

export default feedbackType
