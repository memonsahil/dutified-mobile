type feedbackType = {
    feedbackId: string
    feedbackTitle: string
    userId: string
    userName: string
    userAvatar: string
    feedback: string
    rating: string
    creationDate: Date
}

export default feedbackType
