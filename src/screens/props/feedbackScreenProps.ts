import { RouteProp } from '@react-navigation/native'
import screens from '../params/screens'

type feedbackScreenProps = {
    route: RouteProp<screens, 'Feedback'>
}

export default feedbackScreenProps
