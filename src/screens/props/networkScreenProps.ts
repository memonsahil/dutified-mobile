import { RouteProp } from '@react-navigation/native'
import screens from '../params/screens'

type networkScreenProps = {
    route: RouteProp<screens, 'Network'>
}

export default networkScreenProps
