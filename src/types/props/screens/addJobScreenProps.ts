import { RouteProp } from '@react-navigation/native'
import screens from '../../params/screens'

type addJobScreenProps = {
    route: RouteProp<screens, 'AddJob'>
}

export default addJobScreenProps
